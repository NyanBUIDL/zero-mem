# benchmarks/_legacy

Archived v1.2.0–v1.2.2 work-package benchmark scripts (wp02…wp22), moved by
V132-07 (audit P3-8). NOT evidence-bound anymore: none is imported by live
tests; their acceptance evidence is historical (docs/v1.2.x, artifacts/tasks).

Kept for git-history reproducibility of old evidence runs. Do not import
these from tests or active benchmarks. Inventory + verdicts:
docs/v1.3.2/analysis/BENCHMARKS-INVENTORY.md
