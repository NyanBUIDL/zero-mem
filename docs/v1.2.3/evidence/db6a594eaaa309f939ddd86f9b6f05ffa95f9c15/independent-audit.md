# R-00 Initial Audit Record

This bundle records source-bound R-00 verification inputs. The independent exact-tree review is a required subsequent gate and must replace this provisional record before R-00 is marked VERIFIED.

Confirmed: source-bound v1.2.3 successor verifier exists; manifest source identity is the qualified source ancestor; current evidence commit is derived from `git rev-parse HEAD`; actual bytes are checked against manifest entries and `SHA256SUMS.txt`; evidence delta is allowlisted; Windows/macOS are explicitly NOT_RUN.

Open: clean-checkout reproduction after the evidence commit; fresh independent reviewer verdict; final R-00 status update.
