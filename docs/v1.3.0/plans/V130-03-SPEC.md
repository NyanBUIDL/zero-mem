# V130-03 SPEC — State-as-primary promotion (PROJECT route)

**Finding nguồn:** `docs/v1.3.0/analysis/P1-03-state-as-primary-promotion.md`
**Depends:** — | **Gate:** G0→G2

## Contract

### Change
`src/integration/m7/eligibility.py`: hàm classify nhận thêm `route: Optional[MemoryRoute] = None`. Rule promotion, đặt SAU TOÀN BỘ guard hiện có (non-current lifecycle, subordinate lifecycle, non-promotable types):

```python
primary = primary or (
    route is MemoryRoute.PROJECT
    and resource_type == "state"
    and lifecycle == "active"
)
```

Call site: `evidence_builder.py` truyền route xuống khi gọi eligibility.

### Invariants (scope xác nhận bởi user tại Gate A)
- Promotion KHÔNG override bất kỳ exclusion guard nào — chỉ nâng False→True cho item đã eligible.
- Non-PROJECT routes (SESSION/USER/RESEARCH/None) hành vi byte-for-byte như hôm nay.
- assistant_claim/inference/user_statement không bao giờ primary qua đường này (guard non-promotable đứng trước).
- Budget 5/3/8 giữ nguyên; chỉ phân bổ lại vai trò trong budget.

## Acceptance criteria
1. PROJECT + state active + verification none → as_primary=True.
2. Cùng input, route khác/None → as_primary=False.
3. PROJECT + state superseded/archived → không primary.
4. PROJECT + memory_type=assistant_claim → không bao giờ primary.
5. Budget ≤5/3/8 và estimated_tokens đúng.
6. 3 test Option-B v1.2.4 (`test_m7_3_evidence_builder.py:510-560`) pass KHÔNG SỬA.

## Migration plan
Không cần.

## Rollback plan
Revert commit WP; thuần 2 file (eligibility.py + evidence_builder.py) + tests.

## RISK
- Chữ ký hàm đổi → Graphify impact-set kiểm tra mọi caller của classify; default None giữ backward-compat.
