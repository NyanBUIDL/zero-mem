# DEFECT-REGISTRY — Hệ thống file lỗi bắt buộc của Zero-Mem

> **Vai trò:** Single source of truth cho mọi lỗi (defect) được phát hiện trong Zero-Mem — mở, đang sửa, đã sửa, đã đóng. Mọi fix PHẢI có entry tại đây TRƯỚC khi sửa code, và entry chỉ được đóng khi có bằng chứng test thực tế.
> **Nơi lưu:** `docs/defects/` (registry này + mỗi defect một file `DEFECT-XXX-*.md` khi cần chi tiết).
> **Quy tắc bắt buộc (MUST):**
> 1. Mọi lỗi phát hiện (audit, review, grep, test) → thêm entry vào bảng dưới với trạng thái `OPEN`.
> 2. Mọi fix phải đi theo thứ tự: **DEFECT entry (OPEN) → test RED-first → fix → test GREEN → cập nhật entry (FIXED + evidence) → mới được commit.**
> 3. Không được đóng entry bằng tự báo cáo — phải có lệnh test + kết quả nguyên văn.
> 4. Không xoá entry (append-only); supersede bằng ghi chú `SUPERSEDED-BY`.
> 5. Thiếu DEFECT entry mà có fix code trong commit → commit KHÔNG hợp lệ theo policy này.

## Bảng theo dõi (append-only)

| ID | Phát hiện | Mô tả | Mức độ | Loại | Trạng thái | Fix version | Evidence |
|---|---|---|---|---|---|---|---|
| DEF-001 | 2026-08-23 (GLM 5.3 review) | `budget.py:51` `verified_rank` so tuple `("verified","confirmed")` — KHÔNG thành viên nào của `VerificationStatus` tồn tại trong đó → item verified thật (`direct_tool_output`, `user_confirmation`, `deterministic_verification`, `approval`) KHÔNG BAO GIỜ được rank 0; ranking ưu tiên verified hoàn toàn chết. **Nặng hơn đánh giá cũ** (cũ nghĩ behavior-neutral — sai, vì `"verified"` cũng không phải enum value). | TRUNG BÌNH (retrieval ordering) | Thuật toán/logic enum | FIXED (v1.3.3) | v1.3.3 | RED: `1 failed in 0.38s` (assertion `verification='direct_tool_output' must yield verified_rank 0`) → fix (frozenset từ enum, tái dùng pattern eligibility.py) → GREEN: `51 passed in 0.90s` (DEF-001 4 test + test_m7_3_evidence_builder + test_v130_03_state_primary) |
| DEF-002 | 2026-08-23 (GLM 5.3 review) | `zero_mem/version.py` = `1.3.1` trong khi tag `v1.3.2` đã RELEASED_PUBLISHED → version-state mismatch; sdist/wheel build ra metadata sai version. | THẤP (packaging/audit noise) | Biến/state | FIXED (v1.3.3) | v1.3.3 | `__version__ = "1.3.3"`; pyproject dynamic version (attr zero_mem.version.__version__) — không hardcode nào khác; packaging tests `4 passed` |
| DEF-003 | 2026-08-23 (review; đã ghi nhận trong AUD-003) | M2.6 crash/power-loss durability chưa có proof test (malformed replay đã fail-closed tại `f2cce27`, nhưng chưa có kill-mid-write + journal replay test). | TRUNG BÌNH (durability) | Test coverage gap | CLOSED (v1.3.4) | v1.3.4 | `tests/unit/test_v134_def003_crash_durability.py`: (1) SIGKILL subprocess mid-ingest → canonical JSONL byte-identical + resume deterministic + full replay == clean ingest (logical digest, timestamp-normalized); (2) torn canonical tail (partial last line) never projected + repaired replay converges. 5/5 runs stable. FULL SUITE `3479 passed, 7 skipped, 0 failed` |
| DEF-004 | 2026-08-23 (review; kiến trúc đã ghi nhận) | Knowledge-space grants non-authorizing trong `_scope_allows()` (zm_meta không có cột knowledge_space_id). Fail-closed đúng, nhưng chức năng chưa work. **SCOUT V140-02: chỉ ảnh hưởng event-store path; corpus path ĐÃ hoạt động.** | TRUNG BÌNH (feature gap) | Kiến trúc | **FIXED (v1.4.0, V140-02, Option B)** | v1.4.0 | RED→GREEN `test_v140_02_ks_resolution.py` (9 tests); full suite `3412 passed, 7 skipped, 0 failed` (isolated HOME, Py 3.13.15). Resolution layer `src/access/knowledge_space_resolver.py` + facade expand (no zm_meta schema change, GATE-2 CHỌN B). |
| DEF-005 | 2026-08-23 (review) | `enrichment.py` `KeywordEnrichmentAdapter` viết xong nhưng chưa wire vào pipeline. | THẤP (optional) | Dead-path | OPEN (deferred — chỉ wire khi có retrieval-quality metric) | backlog | — |
| DEF-006 | 2026-08-23 (full-suite run sau DEF-002) | Version bump v1.3.1→v1.3.3 chưa propagate: hardcode `"1.3.1"` còn sót ở 8 chỗ — `release_helpers/release_common.py:127` (manifest pin, làm installer từ chối bundle mới), `tests/unit/test_pkg1_packaging.py:30`, `test_pkg2_packaging.py:159/172/177/179/207`, `test_pkg6_upgrade_lifecycle.py:148`. | TRUNG BÌNH (chặn packaging suite) | Hệ quả DEF-002 / hardcoded pin | FIXED (v1.3.3) | v1.3.3 | RED: pkg1+pkg2+pkg6 = 13 failed → fix 8 pins → GREEN: toàn bộ PASS (xem evidence V133) |
| DEF-007 | 2026-08-23 (full-suite run sau DEF-001) | `zm_verifications` (migrate_7) THIẾU cột provenance `trace_id`/`session_id`/`profile_id` mà mọi bảng M4 khác đều có; projector nhận `VerificationOp.profile_id` nhưng âm thầm drop khi INSERT. Hệ quả: EvidenceItem từ verification rows có `profile_id=None`, `lifecycle=None` (non-enum) → M8.5 scope calibration phân loại nhầm item in-scope là `excluded_unauthorized_scope` (score unavailable), vi phạm closed lifecycle enum mà M8.6 assert. | TRUNG BÌNH (retrieval scoring + authority enum) | Schema/provenance | FIXED (v1.3.3) | v1.3.3 | Migration v12 (additive, rebuildable) + projector persist + VerificationView expose + `_to_evidence_item` normalize None→"active" (match eligibility convention). RED: m8_6 = 9 failed; sau fix chain: FULL SUITE `3477 passed, 7 skipped, 0 failed` (Python 3.13.15, isolated HOME) |
| DEF-008 | 2026-08-23 (báo cáo model ngoài; đã xác minh trên tree) | Dead code `src/retrieval/cursor.py:99-100`: 2 dòng `canonical = json.dumps(...)` + `return hashlib.sha256(...)` lặp sau `return` đầu tiên trong `make_relation_fingerprint`. Không ảnh hưởng behavior (unreachable), chỉ noise audit. | THẤP (code hygiene) | Dead code | FIXED (v1.3.4) | v1.3.4 | Xóa 2 dòng; focused regression test_m3_pagination + test_m3_query: `80 passed` |
| DEF-009 | 2026-08-23 (review; đã xác minh trên tree) | (a) `CorpusSourceRegistry._update_record` đọc toàn bộ JSONL mỗi lần update — O(n)/update, bottleneck nếu registry >10k records; (b) `authorized_read.py:343 fp_request` gán nhầm field name (`profile_id=project_filter`) — behavior-neutral vì chỉ feed cursor fingerprint versioning (scope thật nằm trong `eff_text`), nhưng dễ gây hiểu lầm khi audit. | THẤP→TRUNG BÌNH khi scale | Hiệu suất / đặt tên sai | FIXED (v1.5.0-WP1, ADR-V150-01 Option A) | v1.5.0 | RED-first: `test_v150_wp1_hardening.py` `TestDef009aRegistryIndexRewrite` FAILED trước fix → GREEN sau fix (a) rewrite từ in-memory index (`self._records`), không còn re-read JSONL per update (monkeypatch `Path.read_bytes` đếm = 0 calls); (b) `profile_id=profile_filter`. Focused 8 passed; regression m5/m6/corpus/v140/v141 = **740 passed, 5 skipped**; FULL SUITE **3527 passed, 12 skipped, 0 failed** (74.34s, Py 3.13, isolated HOME). Commit V150-WP1 (local-only). |
| DEF-010 | 2026-08-25 (ox-alpha review V140-02) | Space-grant coarsening: DEF-004 Option B resolve space → tập (profile_id, project_id) rồi authorize mọi event row thuộc cặp đó (`authorized_read.py:_scope_allows` space branch + `knowledge_space_resolver.py`). Hệ quả: grant đọc 1 knowledge_space hẹp thực tế cấp quyền đọc TOÀN BỘ events của (profile, project) sở hữu resource trong space đó — grant space ≡ grant project. Fail-closed đúng, behavior có chủ đích (docstring), chấp nhận được ở mức cá nhân; KHÔNG đạt yêu cầu granular ủy quyền doanh nghiệp. | TRUNG BÌNH (authorization granularity — chỉ nghiêm trọng ở tier enterprise) | Kiến trúc ủy quyền / semantics | FIXED (v1.5.0-WP2, ADR-V150-01 Option B per-row) | v1.5.0 | Phát hiện khóa: `zm_meta.knowledge_space_id` ĐÃ tồn tại từ migration v11 (V130-02) và ingest denormalize sẵn — không cần schema mới. Fix: `_ks_predicate` SQL (`zm_meta.knowledge_space_id IN (...)`) trong `_select_m3` + `_scope_allows` nhận `row_knowledge_space_id` (per-row check ưu tiên; resolution fallback chỉ cho row NULL ks legacy). Space grant giờ authorize ĐÚNG row theo ks của nó — hết coarsening, hết phụ thuộc corpus projection cho row post-v11. RED-first: `test_v150_wp2_per_row_ks.py` 2 FAILED trước → GREEN 3 passed. Regression access/retrieval = **1199 passed**; FULL SUITE **3530 passed, 12 skipped, 0 failed** (77.19s). Commit V150-WP2 (local-only). |
| DEF-011 | 2026-08-25 (ox-alpha review V140-02) | Authorization phụ thuộc derived state: kết quả ủy quyền space-grant giờ đọc từ corpus projection (`zm_corpus_sources`/`zm_corpus_units`) thay vì canonical. Projection stale/thiếu → deny oan (an toàn nhưng khó chịu); projection sai ownership → over-authorize (security-relevant). Lần đầu tiên derived data tham gia lớp bảo mật — ai ghi được projection gián tiếp ảnh hưởng kết quả ủy quyền mà không đụng grants/policy. Rebuildable nên rủi ro kiểm soát được, nhưng cần quy tắc vận hành. | THẤP→TRUNG BÌNH (tier enterprise) | Ranh giới canonical/derived trong authorization | CLOSED (v1.5.0-WP3, ADR-V150-01 appendix — canonical-first per-row only) | v1.5.0 | WP1 gate (MITIGATED) → WP2 per-row ks → WP3 loại trừ gốc rễ: (a) `_scope_allows` bỏ hẳn tham số `space_members`; (b) `_expand_scope_with_spaces` thành NO-OP trên event path (member merging CHÍNH là kênh coarsening + phụ thuộc derived). Event-path space authorization đọc DUY NHẤT `zm_meta.knowledge_space_id` (derived từ canonical JSONL, rebuildable). Resolver+gate giữ cho corpus path (`corpus_unit_search`). Row NULL-ks = unscoped ⇒ không bao giờ được space grant authorize (D-2026-08-22-03). RED-first: `test_v150_wp3_canonical_first.py` 3 FAILED trước → GREEN; 6 test Option B cũ formally superseded (pin absence). Regression **1202 passed**; FULL SUITE **3533 passed, 12 skipped, 0 failed** (75.32s, isolated HOME). |
| DEF-012 | 2026-08-25 (ox-alpha deep review v1.4.0) | DEF-004 Option B resolution layer CHƯA được wire vào bất kỳ constructor production nào: `AuthorizedReadService` nhận `corpus_conn` nhưng mọi điểm dựng service thật (`m6/handlers.py:96 _open_facade`, `m7/injection_adapter.py:284`, `zero_mem_runtime.py:358`, `m7/m8_integration.py:200`) đều không truyền → `_corpus_conn=None` → `_space_members_for()` trả None → space grant trên EVENT path vẫn non-authorizing khi runtime, y hệt trước fix. Tests PASS vì tự dựng service bằng tay (`__new__` + gán tay). Corpus path (corpus_unit_search) hoạt động với space nhờ schema có sẵn cột ks — không phải nhờ resolver. Hệ thống vẫn fail-closed (an toàn), nhưng claim "DEF-004 FIXED" chỉ đúng library-level. | TRUNG BÌNH (feature gap — DEF-004 chưa sống ở runtime; không phải security issue) | Wiring / integration | FIXED (v1.4.1) | v1.4.1 | RED-first: `tests/unit/test_v141_def012_wiring.py` 10 failed / 3 passed trên tree v1.4.0 → fix → GREEN **13 passed**. Full suite: **3515 passed, 6 skipped, 0 failed** (130s). Fix: M6Runtime `corpus_store_path` (precedence flag > env `ZM_M6_CORPUS_STORE_PATH` > XDG userconfig > fail-closed; fail-loud validation `CorpusStoreConfigError` lúc configure/set); `_open_facade` + injection_adapter + zero_mem_runtime truyền `corpus_conn=runtime.open_corpus_conn()`; m8_integration ghi chú fail-closed (không có runtime handle); CLI mới `zero-mem config set/unset/show corpus-store-path` + `zero-mem grant add/list/revoke` bọc `GrantAdminService` (canonical JSONL writer + projection rebuild — không INSERT thẳng DB), validate space tồn tại bằng resolver; doctor thêm check `corpus_authorization`. Commit `69c337b` (local, chưa push). |
| DEF-013 | 2026-08-25 (ox-alpha deep review lần 2 trên tree v1.4.1-WIP; mô tả đầy đủ trong lịch sử commit `69c337b` review) | Grant CLI ghi vào store riêng dưới XDG không nối đường ủy quyền production (`_resolve_grants` đọc derived store chính) ⇒ grant CLI không bao giờ hiệu lực + nguồn sự thật thứ hai cho grants (vi phạm ADR-009). | CAO (admin surface chết + single-source-of-truth violation) | Wiring / canonical boundary | FIXED (v1.4.1-R2, GATE-R1 Option A revert) | v1.4.1 remediation | RED-first: `test_v141_r2_remediation.py::TestDef013GrantCliReverted` FAILED trước fix → **9 passed** sau fix. `cli.py` gỡ wiring grant; `commands_config_grant.py` config-only; 3 test cũ formally superseded (ghi chú trong file, thay bằng pin vắng mặt); admin CLI đa-agent đẩy backlog v1.5+ (ADR-V141-01). |
| DEF-014 | 2026-08-25 (ox-alpha deep review lần 2) | Connection lifecycle: `_resolve_space_or_none` leak (`finally: pass`) + `AuthorizedReadService.close()` không đóng `_corpus_conn`; `_open_facade` tạo conn/request phụ thuộc GC — sidecar chạy dài tích tụ fd. | THẤP→TRUNG BÌNH khi sidecar chạy dài | Resource lifecycle | FIXED (v1.4.1-R2) | v1.4.1 remediation | RED-first `TestDef014CorpusConnLifecycle` 2 FAILED → GREEN: `close()` giờ đóng `_corpus_conn` (guard sqlite3.Error, set None). Regression m5/m6/v122 = 90 passed. Leak (a) biến mất cùng revert phần grant CLI. |
| DEF-015 | 2026-08-25 (ox-alpha deep review lần 2) | Hygiene bundle CLI/doctor: dead code `run_grant_list`, tên hàm sai ngữ nghĩa, assert production, hack except-class, doctor PASS chỉ dựa config tồn tại. | THẤP (auditability) | Code hygiene / observability | FIXED (v1.4.1-R2) | v1.4.1 remediation | Doctor `corpus_authorization` verify LIVE usability (WARN unconfigured / FAIL stale / PASS usable) — RED-first `TestDef015DoctorHonesty`. Phần hygiene còn lại biến mất cùng thu hồi grant CLI. |
| DEF-016 | 2026-08-25 (ox-alpha deep review lần 2) | Verification gap: acceptance test tự dựng service thay vì gọi `_open_facade` thật; evidence lệch số giữa các lần chạy. | THẤP (test confidence) | Test integrity / evidence | FIXED (v1.4.1-R3) | v1.4.1 remediation | Acceptance mới đi qua `_open_facade` thật (env-configured corpus store, migrate_8 schema, resolver end-to-end): `TestDef016RealFacadeAcceptance`. Số chính thức: **3521 passed, 6 skipped, 0 failed ×2 chạy liên tiếp** (.venv-v124 Py 3.11, isolated HOME). Chênh lệch với 3515 trước đó giải thích bằng văn bản: +9 test remediation −3 test superseded. |
| DEF-017 | 2026-08-25 (ox-alpha full-system review v1.4.1) | `tests/unit/test_v140_04_benchmark.py:17` hardcode đường dẫn máy `/home/lenovo/Hermes Workspace/zero-mem-dev-data/corpus-quant-lab/corpus-derived.sqlite` — vi phạm portability (AGENTS.md cấm giả định username/máy); suite trên máy khác mất khả năng chạy benchmark thật dù file tồn tại ở vị trí khác. Có skip-guard nên không fail — chỉ giảm coverage trên máy khác. | THẤP (portability) | Test portability / hardcoded path | FIXED (v1.4.1-R4) | v1.4.1-R4 | RED-first: `test_v141_r4_def017_def018.py` `TestDef017BenchmarkPortability` 2 FAILED trên tree cũ (`"/home/" in text` assert fail) → fix: CORPUS_DB resolve `ZM_TEST_CORPUS_DB` → fallback `(REPO.parent)/zero-mem-dev-data/corpus-quant-lab/corpus-derived.sqlite` (repo-relative, cùng pattern F2 dùng cho test_v140_03) → GREEN. Skip-guard gốc giữ nguyên. |
| DEF-018 | 2026-08-25 (ox-alpha full-system review; tái hiện thật 2026-08-25) | `tests/unit/test_m9_6_hardening.py:146` `Path.home().iterdir()` raise `FileNotFoundError` khi thư mục HOME không tồn tại trên đĩa → full suite fail giả trên môi trường container/home bị xoá (tái hiện: HOME=/tmp/zm-test-home chưa mkdir → `1 failed`; sau mkdir → `23 passed`). | THẤP (test robustness — fail giả môi trường) | Test integrity / edge case | FIXED (v1.4.1-R4) | v1.4.1-R4 | RED-first: `TestDef018HomeAbsentRobustness` FAILED (subprocess pytest với HOME ghost → rc=1 `FileNotFoundError: /tmp/.../ghost-home...`) → fix: guard đầu `test_unconfigured_returns_unavailable_and_creates_nothing` — `Path.home().exists()` False → `pytest.skip` (tính chất "nothing created under HOME" đúng vacuously); assertion gốc khi home tồn tại GIỮ NGUYÊN → GREEN. Focused: 30 passed; FULL SUITE **3519 passed, 12 skipped, 0 failed** (76.66s, Py 3.13, isolated HOME=/tmp/zm-test-home). |
| DEF-019 | 2026-08-25 (post-release verification v1.5.0 @ 10ad8da) | Doctor check `corpus_authorization` nói SAI so với kiến trúc v1.5.0: WARN "not configured — knowledge-space grants on the event path are non-authorizing… Set with: config set corpus-store-path" — nhưng từ WP2/WP3 event path authorize per-row qua `zm_meta.knowledge_space_id`, KHÔNG cần corpus store; corpus store chỉ phục vụ corpus_unit_search. Hệ quả: hướng dẫn vận hành sai (user nghĩ phải config corpus store mới dùng được space grant) — cùng loại sai lệch self-report mà DEF-015 từng sửa. Không ảnh hưởng security/data, chỉ message. | THẤP→TRUNG BÌNH (observability / misleading guidance) | Observability / doctor honesty | FIXED (v1.5 post-release additive, commit trên master sau 10ad8da) | v1.5.x | RED-first: `test_v150_r1_doctor_honesty.py` 2 FAILED (message chứa "non-authorizing" + khuyên set corpus-store-path cho event path) → fix `commands_doctor.py`: unconfigured = PASS với mô tả per-row canonical; configured = PASS/FAIL theo usability CHO corpus_unit_search, ghi rõ event grants không ảnh hưởng. Test DEF-015 cũ superseded (WARN→PASS re-pin). Baseline duplicate-key catch: v150_status ghi 2 lần → gộp 1. FULL SUITE **3537 passed, 12 skipped, 0 failed** (76.42s). Tag v1.5.0 KHÔNG đụng — fix additive trên master. |
| DEF-020 | 2026-08-25 (audit V1.5.0 so với ZERO-MEM-v1.5.1-REMEDIATION-ROADMAP WP-01) | FTS `search_text` KHÔNG mang `knowledge_space_id` qua `SearchHit` và KHÔNG đưa KS predicate vào candidate SQL; sidecar FTS path (`AuthorizedReadService.search_text`) chỉ truyền `req.knowledge_space_id` (filter chuẩn) chứ không mang effective scope (grant space). Hệ quả: cross-profile space grant không authorize được FTS hit, và event cùng profile/project nhưng khác KS KHÔNG bị lọc ở SQL — trái ngược với structured path đã sửa ở DEF-010/011. Structured và FTS dùng HAI semantics authorization khác nhau. | CAO (authorization parity) | Authorization / retrieval | FIXED (v1.5.1-WP01) | v1.5.1 | RED-first `test_v151_wp01_fts_ks_auth.py` trên baseline → GREEN sau fix (`_effective_scope_predicate` vào candidate SQL qua `candidate_where/candidate_params`, `SearchHit.knowledge_space_id`, `fingerprint_extra="v151|"+clause+params+grant_refs`). Regression m5/m8/v150 = 88 passed trong B5 review; FULL SUITE 3555/12/0 ×2 |
| DEF-021 | 2026-08-25 (audit V1.5.0 so với roadmap WP-02) | (a) `AuthorizedReadService.query_events` gọi `_select_m3(scope, None, ...)` → `_validate_limit(None)` trả `-1` → SQL `LIMIT -1` (không giới hạn) rồi `fetchall()` TOÀN BỘ result set trước khi cắt `eff_limit` ở Python; vi phạm "query page đầu không fetch toàn bộ bảng". (b) Invalid limit bị silent default/clamp: `limit=0`/`negative`/`>MAX`/`bool`/`string` KHÔNG raise mà bị ép về DEFAULT/MAX trong facade (structured path `_validate_limit` đúng raise, nhưng facade ghi đè). | CAO (memory/time bound + limit integrity) | Hiệu suất / input validation | FIXED (v1.5.1-WP02) | v1.5.1 | RED-first `test_v151_wp02_limit_and_fts_ceiling.py::TestDef021StrictLimit` (6 bad limits raise `invalid_limit`) + `test_limit_none_uses_default` (default 50 = server ceiling, cursor minted). SQL trace sau fix: `limit=1`→`LIMIT 2`, 0 unbounded zm_meta SELECT (`docs/v1.5.1/EVIDENCE.md` §B3); tracemalloc single-page peak flat 20.6KB từ 1k→100k rows |
| DEF-022 | 2026-08-25 (audit V1.5.0 so với roadmap WP-02) | `AuthorizedReadService.search_text` chạy một query FTS cho MỖI scope (base + mỗi grant) rồi nối kết quả ở Python, mỗi query lấy `limit` row độc lập → có thể trả `scope_count × limit` item (vượt ceiling). `next_cursor=None` luôn (không phân trang union). Mỗi hit chỉ kiểm tra `_scope_allows` post-hoc, không lọc candidate ở SQL. | CAO (limit ceiling + pagination) | Authorization / retrieval | FIXED (v1.5.1-WP02) | v1.5.1 | RED-first `TestDef022FtsCeiling`: multi-scope 2×10 events limit=10 → đúng 10 (không 20); union paginate 20 pages ×limit=7 không duplicate, đủ cả hai KS; invalid FTS limit raise. Single candidate query với effective-scope predicate; cursor fingerprint gắn `v151|`+scope clause |
| DEF-023 | 2026-08-25 (audit V1.5.0 so với roadmap WP-03) | `CorpusSourceRegistry.register_source_with_blob` happy path: `register_source` append 1 dòng, sau đó `_update_record` REWRITE TOÀN BỘ registry JSONL (N dòng) chỉ để bind `blob_ref` → ingest N source thành O(N²) I/O (dù DEF-009a đã bỏ re-read per-update, vẫn còn full rewrite per ingest). Không phải O(1) amortized. | TRUNG BÌNH (ingest scalability) | Hiệu suất | FIXED (v1.5.1-WP03) | v1.5.1 | RED-first `test_v151_wp03_registry_blob.py` (blob-first ordering, idempotent retry, orphan-only failure, restart reconstruct). Benchmark ngoài repo (`zero-mem-dev-data/evidence/v151f-b3-registry-bench.out`): per-source 0.184→0.111 ms FLAT N=100→5000; bytes-written old-model O(N²) ×10/×50/×101/×502 vs new append-only ⇒ O(1) amortized |
| DEF-024 | 2026-08-25 (audit V1.5.0 so với roadmap WP-05) | Không có CI workflow trigger trên `master`/PR/tag. Workflow duy nhất (`v1.2.4-qualification.yml`) gắn branch `release/v1.2.4` đã đóng. V1.5.0 phát hành thủ công không qua CI tự động. Thiếu test extra (`PyYAML` cho `check_machine_state.py` parse YAML). | CAO (release integrity / regression gate) | CI / tooling | FIXED-CODE (v1.5.1-WP05; CI xanh chờ Gate PUSH) | v1.5.1 | `.github/workflows/v1.5.1-qualification.yml` mới: ubuntu/windows/macos × 3.11/3.12/3.13, triggers push/PR/tag/workflow_dispatch, test extra `test = ["pytest","PyYAML"]`, `ci = ["build"]`. `test_v151_wp05_ci_packaging.py` GREEN. Remote 9-cell XANH là điều kiện riêng của Gate PUSH — chưa claim |
| DEF-025 | 2026-08-25 (audit V1.5.0 so với roadmap WP-06) | (a) README mở đầu "Zero-Mem v1.2.0 is a local-first evidence and memory sidecar" dù artifact là V1.5.1. (b) `pyproject.toml` dùng `license = { text = "MIT" }` (table) — sẽ thành lỗi bắt buộc ở setuptools 2027; roadmap yêu cầu chuyển sang SPDX string. (c) `project-state.yaml` baseline ghi duplicate key `v150_status` (đã bắt ở DEF-019, cần giữ sạch). | THẤP (evidence honesty / packaging future-proof) | Docs / packaging | FIXED (v1.5.1-WP06) | v1.5.1 | README diff: "Zero-Mem v1.2.0 is…" → "Zero-Mem is…"; pyproject diff: `license = { text = "MIT" }` → `license = "MIT"` (SPDX); duplicate key giữ sạch theo DEF-019 (`check_machine_state.py`: machine state OK); version bump 1.5.1 đồng bộ pin (packaging 28 passed) |
| DEF-026 | 2026-08-25 (V1.5.1-Finish B1 recon — Graphify snapshot `zero-mem-dev-data/graphify/v151-finish-b1` + grep ownership + probe thực nghiệm Py 3.13.15) | **Kết luận sau điều tra đầy đủ: NO-DEFECT về hành vi — đăng ký để ghi nhận semantics + test pin (đã đóng CLOSED-no-defect).** Nghi vấn ban đầu: async timeout path của `AsyncClient._call` (`asyncio.wait_for` trên concurrent future) dùng deferred cancellation → abandoned work tích tụ trong work-queue và chạy ngầm sau deadline, ghi canonical JSONL sau khi caller nhận `AsyncTimeoutError`. Điều tra quyết định (probe v5, real PublicClient + blocking writer): **cancel propagation là THẬT** — `asyncio.wait_for` timeout cancel wrapper-future, wrapper cancel lan xuống concurrent.Future CHƯA start (`future.cancel()` → state CANCELLED), `_WorkItem.run()` thấy `set_running_or_notify_cancel()==False` → no-op, không có side effect canonical nào sau deadline (20 timeouts → 0 event ghi ngầm). Work-item rác vẫn nằm trong SimpleQueue cho tới khi worker dequeue nó (O(1) mỗi item, không re-run), queue size = số timeout trong lúc worker bận nhưng KHÔNG tăng vô hạn theo thời gian sống process — chỉ theo burst timeout, mỗi item bị gỡ O(1) khi worker rảnh. Thread count bounded (=max_workers=1). Sync sidecar tương đương: admission giữ slot đến work xong (100×timeout → admitted 0, futures 0). Per-request SQLite connection ownership chưa proven → cancellation trong worker vẫn không an toàn, nhưng không cần: cancel-before-start đã đủ vì operation chưa chạm storage. | TRUNG BÌNH→THẤP (deferred-cancellation semantics đã xác minh đúng; memory residue của cancelled _WorkItem là bounded per-burst) | Concurrency / lifecycle | CLOSED (no-defect — behavior verified correct; tests pin added) | v1.5.1 | RED-phase probes verbatim: `zero-mem-dev-data/evidence/v151f-b1-async-probe.out` (98 timeout / qsize 98 / thread 1); decisive `v151f-b1-execution-probe5.out` (total_appended_events: **1**, expected 21 nếu abandoned work chạy — chứng minh cancel-before-start thật); control sync `v151f-b1-saturation-probe.out` (admitted 0, futures 0); CPython semantics `v151f-b1-cancel-probe3/4.out`. Pin tests: `tests/unit/test_v151_phase4_async_cancellation.py` (2 passed). |
| DEF-027 | 2026-08-25 (V1.5.1-Finish B3 performance evidence — SQL trace + pagination walk trên fixture 300 events, Py 3.13.15) | **Structured keyset pagination MẤT DỮ LIỆU sau trang 1.** `AuthorizedReadService.query_events` decode cursor và extract keyset (`authorized_read.py:466-468`) nhưng KHÔNG BAO GIỜ truyền `keyset` cho `_select_m3()` (call site `:474-480` thiếu tham số) — mỗi scope query fetch lại TỪ ĐẦU result set với window `LIMIT eff_limit+1`; bộ lọc Python-side `:493-495` chỉ giữ row SAU keyset, nên toàn bộ row nằm giữa ranh giới trang và cuối window bị bỏ vĩnh viễn. SQL trace xác nhận: page-0 query có `(created_at,event_id) > (?)`, page-1 query KHÔNG. Walk 300 events limit=5: chỉ 2 pages / 6 unique events thay vì ~61 pages / 300. FTS path (`search_text`) KHÔNG bị ảnh hưởng — `_run_match` nhận keyset đúng. | CAO (data integrity / pagination correctness — silent data loss trên public read API) | Retrieval correctness | FIXED (v1.5.1-Finish) | v1.5.1 | RED-first `test_v151_finish_def027_keyset.py`: 2 FAILED (walk 6/300, page-2 sai) → fix 1 dòng (`keyset=keyset` vào call-site `_select_m3`) → GREEN 2 passed + regression 201 passed (m3_pagination/m3_query/m5/m8_3/v151_wp01/wp02/v150_wp2/v130_02); repro GREEN: pages 60, 300/300 unique; FULL SUITE 3555/12/0 ×2 |
| DEF-028 | 2026-08-25 (dsh V1.5.1 static audit — DEF-A1) | **Base-scope KS request bị đối xử như GRANT scope → cross-profile leak.** (a) `_profile_predicate` (authorized_read.py:123-126) trả `(None,[])` cho MỌI scope có ks (comment 'grant scope' nhưng base scope chạm nhánh) → SQL không profile clause; (b) `_scope_allows` is_grant_scope conflation (authorized_read.py:179) → nhánh KS authorize mọi profile; (c) `corpus_unit_search` enumeration sinh `(None,None,K)` (authorized_read.py:924-930) → corpus path **LIVE** (units mang ks trong production qua corpus_generic_ingest). Event path **armed latent** (envelope hỗ trợ ks — ingest denormalize; capture chưa set). Hệ quả phụ: include_global+KS → false-negative deny row của chính requester; get_event vs query_events không đồng nhất error-vs-empty. | CAO (P1 — authorization boundary; corpus LIVE, reachable từ MCP/Hermes/public surface) | Authorization / scope semantics | FIXED (chờ full suite — RED→GREEN đã chạy) | v1.5.1 | RED-first `tests/unit/test_v151_audit_a1_base_ks_scope.py` (5 case): 4 FAILED trên baseline (leak event+get_event+corpus khi `include_global=False`; false-negative default) → fix: thêm `AllowedScope.is_grant` (contracts.py), grant scopes set `is_grant=True` (grants.py), `_profile_predicate`/`_scope_allows`/corpus enumeration chỉ cho grant scope profile-unrestricted (authorized_read.py) → GREEN 5 passed; regression auth/retrieval 363 passed; 2 test cập nhật theo semantics mới (base scope requester-scoped; hand-built grant scope cần `is_grant=True`). Chi tiết: `audit/DEF-028-FIX-RECORD.md` |
| DEF-029 | 2026-08-25 (dsh V1.5.1 static audit — DEF-B1) | Facade structured cursor fingerprint thiếu `project_filter` + `created_at_after/before` (authorized_read.py query_events: `fp_request` chỉ có profile/session/verification/lifecycle; `_select_m3` thực thi thêm project+time) → cursor minted dưới filter set A được chấp nhận lại dưới filter set B (cùng fingerprint) → keyset áp lên filter khác → **skip row / continuation sai** (họ DEF-027). Không bypass auth. | TRUNG BÌNH (P2 — pagination integrity) | Retrieval / cursor binding | FIXED (chờ full suite — RED→GREEN đã chạy) | v1.5.1 | RED-first `tests/unit/test_v151_audit_b1_cursor_filters.py`: 2 FAILED (cursor reuse qua project_filter/time-window không bị từ chối; control test pagination PASS) → fix: thêm `project_id=proj` + `created_at_after/before` vào `fp_request` (authorized_read.py query_events) → GREEN 3 passed; regression auth 363 passed chung DEF-028 run. Chi tiết: `audit/DEF-029-FIX-RECORD.md` |
| DEF-030 | 2026-08-25 (dsh V1.5.1 static audit — DEF-C1; OXALPHA F7) | Corpus retrieval: (a) FTS discovery (retrieval.py:362-378) KHÔNG LIMIT → mọi row match materialize vào Python trước auth-filter; (b) `_read_all_units` (retrieval.py:308) full-table scan không WHERE/LIMIT. Fail-safe auth (filter trước ranking) nhưng memory spike / DoS-kiểu-tai-nạn trên query rộng khi MCP server mở cho client ngoài. | TRUNG BÌNH (P2 — memory/DoS) | Hiệu suất / bounded query | FIXED (chờ full suite — RED→GREEN đã chạy) | v1.5.1 | RED-first `tests/unit/test_v151_audit_c1_corpus_bounded.py`: 2 FAILED (FTS discovery + metadata-only SQL không LIMIT) → fix: `_read_all_units(cur, cap)` + FTS discovery `LIMIT ?` với `cap = plan.limit * 50` (`_DISCOVERY_FACTOR`, retrieval.py) → GREEN 3 passed + corpus regression 29 passed. Chi tiết: `audit/DEF-030-FIX-RECORD.md` |
| DEF-031 | 2026-08-25 (dsh V1.5.1 static audit — DEF-C2; OXALPHA F5/F6) | Corpus scoring: (a) `_lexical_score` (retrieval.py) TF-only, không IDF, không length-norm (authorized-subset IDF là hợp pháp — deterministic per-scope, không phá authorization-before-influence; **giữ nguyên — backlog**); (b) `_fts_safe_query` AND-only, không OR-fallback — M3 event FTS CÓ OR fallback (search.py V130-01), corpus path không → driver precision@1=0.095 (query nhiều từ thiếu 1 token → miss tuyệt đối). | TRUNG BÌNH (P2 — retrieval quality) | Thuật toán / retrieval | FIXED (phần OR-fallback; IDF/length-norm backlog — chờ benchmark lớn) | v1.5.1 | RED-first `tests/unit/test_v151_audit_c2_or_fallback.py`: 1 FAILED (AND miss) → fix `_fts_or_query` + precision-guarded OR fallback (chỉ khi AND=0 rows && >=2 terms; bound parameter, parity search.py V130-01) → GREEN 3 passed + corpus 29 + bounded 3 = 35. **Benchmark**: AND-only recall 0/10 → OR-fallback 10/10 (10/10 query recovered, 1.9ms/10). Chi tiết: `audit/DEF-031-FIX-RECORD.md` |
| DEF-032 | 2026-08-25 (dsh V1.5.1 static audit — DEF-D1) | **CLOSED no-defect (điều tra đầy đủ bằng executable).** Nghi vấn ban đầu: `CorpusSourceRegistry._update_record` (registry.py:241-292) là dead code — grep toàn src chỉ thấy định nghĩa, không caller production. **Điều tra bổ sung:** method có TEST COVERAGE trực tiếp — `test_v150_wp1_hardening.py` gọi `reg._update_record(...)` (dòng 66, 80) assert không re-read file (DEF-009a); `test_v151_wp03_registry_blob.py` assert blob path không gọi nó (happy path O(1)). Đây là đường repair/rebind legacy có contract + test; KHÔNG phải dead code. Kết luận: NO-DEFECT — giữ nguyên. | THẤP (P3 — hygiene) | Dead code | CLOSED (no-defect — test coverage tồn tại) | v1.5.1 | `test_v150_wp1_hardening.py` + `test_v151_wp03_registry_blob.py` = 11 passed (0.96s) |
| DEF-033 | 2026-08-25 (dsh V1.5.1 static audit — DEF-E1; remediation plan Phase 4 'register a dedicated defect if one is absent') | Sync sidecar (src/integration/sidecar.py handle): `future.result(timeout=...)` → FutureTimeout trả DEADLINE_EXCEEDED nhưng **future không bị cancel** → queued-not-started work vẫn chạy sau deadline, admission slot giữ tới khi work xong (async path `zero_mem/api.py` CÓ cancel-before-start — DEF-026 verified; sync path thiếu tương đương). Không có metrics phân biệt 'caller timed out' vs 'work still running' (hypothesis E prompt). | THẤP→TRUNG BÌNH (P3 — resource lifecycle + observability) | Concurrency / observability | FIXED (chờ full suite — RED→GREEN đã chạy) | v1.5.1 | RED-first `tests/unit/test_v151_audit_e1_sidecar_cancel.py` (3 case): 2 FAILED trên baseline (queued future chạy sau timeout; không metrics) → fix `src/integration/sidecar.py`: `future.cancel()` trên FutureTimeout (cancel-before-start, parity async path) + `metrics` property (caller_timed_out/work_still_running/queued_cancelled_before_start/deadline_exceeded/overloaded) → GREEN 3 passed + sidecar/m6 regression 53 passed. Chi tiết: `audit/DEF-033-FIX-RECORD.md` |
| DEF-034 | 2026-08-25 (dsh static + executable probe; **review 2026-08-25 thu hẹp verdict**) | **CONFIRMED — standard capture adapters không truyền KS vào top-level canonical envelope.** `normalize_event` (adapter.py:41-74) + `_envelope` (capture_adapter.py:58-98) dựng whitelist cố định, không `knowledge_space_id` (KS qua payload rơi vào `sanitized_content.extra` — probe H). Capture STORE/boundary KHÔNG drop: manual top-level ks trước append → canonical giữ + ingest denormalize (probe I). Hệ quả: event Hermes-captured → zm_meta.ks NULL → space grant event path fail-closed deny (feature gap, không leak trong các path đã test — probe J: global read trên NULL-profile bỏ qua ks theo D-2026-08-22-03, ks loss không mở rộng exposure). **KHÔNG phải 'chỉ mất tại capture'**: downstream gaps riêng — `list_knowledge_space` trả rỗng (relations.py:251-270, probe K), M8 graph gán ks=None (graph_sources.py, probe L), projection hardcode `[]` (render.py:516). Schema SINGULAR (migrate_11) — không multi-KS/event. | TRUNG BÌNH (P2 — feature gap; security: no-leak-observed trong tested paths, cần gate NULL/legacy/global-read trước khi claim) | Wiring / integration + single-KS limitation + downstream parity | **OPEN → V1.6.0 (KHÔNG FIXED trong V1.5.1 — không migration, không schema change)** | V1.6.0 | Tests evidence: `tests/unit/test_v151_audit_def034_lifecycle.py` + `test_v151_audit_def034_downstream.py` (14 evidence tests / 59 total related passed (incl. v130_02 + m3_relations)); `docs/defects/DEF-034-ROOT-CAUSE.md`; `docs/v1.6/ADR-V160-01-MULTI-KS-PROPOSAL.md`; `docs/v1.6/V160-MULTI-KS-REMEDIATION-PLAN.md`; probes `docs/v1.6/probes/` **Phạm vi:** commit evidence DEF-034 KHÔNG đổi production code (docs/tests only); branch chứa fix DEF-028/029/030/031/033 từ trước. |
| DEF-035 | 2026-08-25 (dsh V1.5.1 WP-05 chạy full suite Windows) | Symlink-based security tests lỗi trên Windows khi không có Developer Mode/admin (WinError 1314): test_m9_1_paths/config, m9_2, m9_4, wp25, wp27, v122_platform_storage, wp33. Đúng plan Phase 5: capability-probe + skip có reason, không blanket-skip cả file (security tests không phụ thuộc symlink vẫn chạy). | THẤP→TRUNG BÌNH (CI portability) | Test portability / Windows | FIXED (v1.5.1-WP05) | v1.5.1 | `tests/unit/_symlink_guard.py` (require_symlinks: os.name!='nt' -> return; probe symlink -> skip khi thiếu privilege); gắn vào 25+ test symlink (class fixture TestSymlinkEscape + per-test calls). Full suite: 38 skipped (từ 16), các test symlink SKIP có reason rõ. |
| DEF-037 | 2026-08-26 (dsh V1.6.0 reconcile - full suite, standalone python 3.13.15) | **CLOSED no-defect (root-cause executable - ENVIRONMENT_CAPABILITY, khong phai defect code).** test_pkg2_packaging.py (11 tests) ERROR tai setup voi loi: python -m venv <bundle-build> tra non-zero. **Probe root-cause:** python -m venv tao venv xong nhung venv-created python chay -m ensurepip FAIL (exit 1) - quirk cua python-build-standalone base (venv template co ensurepip broken); base python ensurepip --version = pip 26.2.1 OK. Installer-flow tests can venv hoat dong de cai bundle; standalone base khong tao duoc venv hoat dong -> environment capability gap cua test harness, khong phai defect Zero-Mem. **Baseline executable:** 11 errors y het xuat hien trong moi full-suite log truoc C4 va trong C4-era (fullsuite-pre-pinfix-8failed.txt, fullsuite-post-pinfix-6failed.txt, fullsuite-post-followup-6failed.txt, c4-fullsuite-6failed.txt, c4fu-fullsuite-6failed.txt) - pre-existing, khong phai regression. Luu y: tren moi truong python khac (reviewer chay doc lap) cung tests cho 8 FAILED thay vi 11 ERROR - hanh vi khac theo python install, cung co ket luan environment-dependent. | THAP (P3 - test env capability) | Test environment / packaging | CLOSED (no-defect - environment capability; root-cause probe + baseline) | v1.6.0 reconcile | Root-cause probe: python -m venv <dir> exit 1 (venv-created ensurepip fail) + python -m ensurepip --version = pip 26.2.1 OK; full-suite log reconcile-fullsuite.txt (11 pkg2 errors). |

> **DEF-037 ADDENDUM (2026-08-26, reconcile round-2 — REOPENED / INVESTIGATING):** prior kết luận `CLOSED no-defect — root cause verified` (standalone python-build-standalone không tạo được working venv) **KHÔNG tái hiện trên môi trường sạch**: reviewer chạy lại chính `.venv\Scripts\python.exe` (Python 3.13.15) → `python -m venv <dir>` exit 0, venv-created python `-m ensurepip --version` exit 0 (pip 26.2.1), `-m pip --version` exit 0 — venv creation HOẠT ĐỘNG. Root cause cũ bị bác bỏ (không phải defect standalone-python).
> **Probe mới (dsh sandbox, cùng `.venv` python):** `python -m venv` exit 1 — bước install `ensurepip --upgrade --default-pip` fail với `PermissionError: [Errno 13] Permission denied` + `WinError 5 Access is denied` trên temp dir 0o700 (`tempfile.TemporaryDirectory` tạo `tmpXXXX_` rồi `os.chmod(0o700)`, sandbox DSH chặn ghi vào dir 0o700); fresh venv KHÔNG kế thừa sitecustomize ACL patch của parent venv → ensurepip install fail; venv-created python `-m pip` = `No module named pip`. `test_pkg2_packaging.py` = 2 passed / 11 errors (tái hiện trong sandbox). Full-suite reviewer (env sạch) = 3602 passed / 9 failed / 38 skipped — 8 pkg2 FAILED, KHÔNG có 11 setup errors.
> **Kết luận hiện tại:** hiện tượng 11 pkg2 setup errors là **environment-sensitive** (chỉ tái hiện trong DSH sandbox do 0o700-temp + write denial, không phải defect Zero-Mem, không phải defect standalone-python). Trạng thái DEF-037: **REOPENED / INVESTIGATING** — không giữ claim CLOSED; chưa đóng cho tới khi có executable split rõ giữa sandbox-vs-clean env (chạy pkg2 suite trên một supported Python ngoài sandbox). Không sửa production code trong phiên này. Evidence: `def037-probe.out`, `def037-pkg2.txt` (workspace root `.tmptest`).

> **DEF-037 ADDENDUM ROUND-3 (2026-08-26) — ROOT CAUSE UNCONFIRMED (round-2 overclaim):** round-2 addendum đã overclaim — nó tuyên bố root cause cụ thể (sandbox 0o700 write denial / fresh venv thiếu sitecustomize ACL patch / không phải defect Zero-Mem) trong khi raw evidence được dẫn (`def037-probe.out`, `def037-pkg2.txt`) chỉ chứng minh outer sequence: venv exit 1 + outer stderr (ensurepip subprocess exit 1), venv python tồn tại, ensurepip --version exit 0, pip absent, pkg2 2 passed / 11 errors. Raw evidence KHÔNG chứa PermissionError/WinError 5/0o700/ACL proof.
> - Observed (verbatim): (a) một sandbox run: venv exit 1, pip absent, pkg2 = 2 passed / 11 errors; (b) clean reviewer run (cùng `.venv` python 3.13.15): venv/ensurepip/pip đều exit 0; (c) DIRECT retry trên previously failed venv của reviewer: `venv\Scripts\python.exe -m ensurepip --upgrade --default-pip` → `Successfully installed pip-26.2.1` exit 0; (d) reviewer full suite: 8 pkg2 FAILED thay vì 11 setup errors.
> - Classification: **ENVIRONMENT_SENSITIVE / NON-DETERMINISTIC.** Root cause: **UNCONFIRMED.** Zero-Mem defect status: **NOT YET DETERMINED.** Standalone-Python defect status: **NOT YET DETERMINED.** Không được giữ các câu khẳng định round-2: "sandbox 0o700 write denial là root cause đã verified"; "fresh venv thiếu sitecustomize ACL patch là root cause"; "không phải defect Zero-Mem". Permission/ACL/0o700 chỉ là hypothesis (một traceback permission-lỗi từng được quan sát trong console một lần của run sandbox, nhưng KHÔNG lưu thành raw file và KHÔNG tái hiện được qua retry của reviewer).
> - Trạng thái giữ: **REOPENED / INVESTIGATING.** Next evidence yêu cầu: inner ensurepip full stderr (raw file) của một failed run; supported-Python split test ngoài sandbox; nếu inner stderr không lấy được thì ghi `INNER_STDERR_NOT_CAPTURED` và không suy đoán. Không sửa production/test code trong vòng này.
| DEF-036 | 2026-08-25 (dsh V1.5.1 full suite Windows) | **CLOSED no-defect (điều tra probe đầy đủ).** Nghi vấn: async cancel-before-start không giữ trên Windows — `test_v151_phase4_async_cancellation` (deadline=0.002s) ghi 9 events sau 20 timeout (assert ≤2; Linux ≤2). **Probe quyết định** (real AsyncClient + SlowWriter, 20 observe deadline 0.002/0.01/0.05/0.1): deadline 0.002 → 7 events; 0.01 → 11 events (variance); 0.05/0.1 → 0 timeout (deadline > 30ms write). Kết luận: **không phải lỗi cơ chế cancel** — Windows default timer resolution ~15.6ms làm deadline 2ms/10ms được trả TRỄ, executor kịp bắt đầu 7-11 item trước khi timeout; số events bounded (không 20, không vô hạn) → cancel-before-start vẫn hoạt động cho future CHƯA start, chỉ là window nhỏ hơn do granularity. Test dùng deadline dưới timer resolution của Windows là Linux-tuned. Không sửa test maintainer; ghi limitation: trên Windows, deadline < ~15ms không đảm bảo cancel trước start. | THẤP (P3 — test timing granularity; cơ chế bounded) | Concurrency / async / platform | CLOSED (no-defect — mechanism verified bounded; timing artifact) | v1.5.1 | Probe `audit/DEF-036-PROBE.md`: 0.002→7, 0.01→11, 0.05/0.1→0 timeout; events bounded theo deadline, không unbounded. |

## Addendum V1.6.0 C10 — 2026-08-27

> **DEF-037 ROUND-4 — CONFIRMED Zero-Mem Windows junction defect:** supported Python 3.12.13 creates working venvs and reaches the PKG-2 assertions; the suite consistently reports 8 failures rather than setup errors. Executable junction probe reproduces the product defect: `_make_dir_link(link, Path("runtimes") / version)` falls back to `mklink /J` with `cwd=Path.home()`, so Windows resolves the relative target under the user home instead of `link.parent`. Probe output: `is_junction=True`, `is_symlink=False`, resolved target `C:\Users\brian\runtimes\1.5.1`, expected `<runtime-root>\runtimes\1.5.1`. The installer/uninstaller also recognize only `is_symlink()`, although Python 3.12 exposes junctions separately through `Path.is_junction()`. Status: **CONFIRMED / FIX IN C10**. Boundary: activation-pointer creation/detection/removal and the Windows-specific acceptance assertion; canonical data is not involved.

> **DEF-038 — OPEN (test portability / crash durability):** on Windows, immediately reopening a WAL database read-only after `proc.kill()` can transiently raise `sqlite3.OperationalError: disk I/O error`; the same file becomes readable shortly afterwards and remains integrity-checkable. The existing DEF-003 test performs a single immediate read-only open, so it can fail before exercising resume/rebuild semantics. Status: **OPEN / FIX TEST HARNESS IN C10**. Smallest fix: bounded Windows-only retry for this exact transient error; all other SQLite errors continue to fail closed.

## Quy trình fix chuẩn (per defect)

```
1. Ghi DEFECT entry (mô tả, root cause, phạm vi ảnh hưởng, biên an toàn).
2. Viết test thất bại trước (RED-first) bọc đúng hành vi sai.
3. Sửa code — thay đổi nhỏ nhất (smallest change), không mở rộng phạm vi.
4. Chạy test tập trung (focused test) → PASS.
5. Chạy full suite (isolated HOME) → 0 fail.
6. Cập nhật trạng thái + evidence + commit (thông điệp tham chiếu DEFECT-ID).
7. Chỉ đóng (CLOSE) khi có bằng chứng full-suite.
```

## Tech-stack fix (định hướng kỹ thuật cụ thể)

- **DEF-001:** tái sử dụng `_VERIFIED_STATUSES` pattern từ `eligibility.py:183` (import `VerificationStatus` từ `src/capture/event_types.py`, frozenset loại trừ `"none"`). KHÔNG hardcode chuỗi. Test: construct `EvidenceItem` với `verification="direct_tool_output"` vs `"none"`, verify `_order_key` ranking và `select_evidence` ordering. Python stdlib + pytest, không thêm dependency.
- **DEF-002:** bump `__version__` → `"1.3.3"`; đồng bộ `pyproject.toml` nếu có hardcode; verify qua import + kiểm tra metadata build nếu build trong scope.
- **DEF-003 (v1.3.4):** harness `subprocess` kill -9 mid-ingest + replay JSONL → assert digest; stdlib only.
- **DEF-004 (v1.4.x):** ADR trước — chọn giữa (A) thêm cột `knowledge_space_id` vào zm_meta (migration v11, schema-first) hoặc (B) resolution layer ánh xạ space → resource ids từ derived state. Không code trước ADR.
- **DEF-006:** mọi version pin đi qua `zero_mem.version.__version__` (single source); test pins dùng cùng import, KHÔNG hardcode lại trong lần bump sau.
- **DEF-007:** migration v12 additive (`ALTER TABLE ADD COLUMN` ×3, idempotent theo PRAGMA table_info); `_recreate_m4_tables()` phải re-apply migration mới nhất sau migrate_7 khi thêm migration tương lai; VerificationView expose provenance + lifecycle marker "active" (convention ProjectArtifactView).

## Lịch sử

- 2026-08-23: registry tạo theo chỉ đạo của maintainer (chat, 2026-08-23) — quyền tự quyết full-cycle cho gói v1.3.3 (fix DEF-001/002 → test → publish GitHub v1.3.3) được cấp trong chat, ghi vào mutation record.
