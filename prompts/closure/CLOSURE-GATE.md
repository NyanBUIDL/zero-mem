# Closure Gate

Read the frozen WP gates before closure. Run the named focused suite, state/control tests, full regression, identity checks, Product Memory boundary audit, later-WP untouched audit, and `git diff --check`. Bind final evidence to the final state hash and final local checkpoint SHA. Do not invent a new architecture requirement during closure.
