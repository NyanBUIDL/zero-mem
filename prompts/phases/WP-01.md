# WP-01 Phase Prompt

## Objective

Freeze the architecture contract from surviving planning and direct source evidence. Do not implement WP-08 API signatures or WP-07/WP-21 adapters in this WP.

## Authorized paths

`artifacts/tasks/WP-01.md`, `artifacts/codemaps/WP-01-architecture.md`, `artifacts/evidence/WP-01-*.md`, `artifacts/handoffs/WP-01-*.md`, `prompts/phases/WP-01.md`, and the namespaced v1.1 overlay in `project-state.yaml`.

## Forbidden paths

`zero_mem/**`, `src/**`, `tests/**`, `packaging/**`, dependency metadata, runtime configuration, Product Memory, and remote Git.

## Required production call graph

Record adapter caller, authority boundary, implementation owner, integration point, and downstream consumer. A helper-only change is not completion.

## Closure

Run control validation, full regression, identity checks, forbidden-path audit, and `git diff --check`. Create one local checkpoint before WP-02.
