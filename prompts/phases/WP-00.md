# WP-00 Phase Prompt

## Objective

Reconstruct a durable v1.1 control plane from the surviving planning authority without claiming continuity with `/tmp/zero-mem`.

## Scope

Control-plane artifacts, machine state, task manifest, prompt registry, Development Memory, baseline evidence, Graphify evidence, and handoff. No Product Memory ingestion. No remote Git.

## Frozen closure gates

- baseline/planning/canonical identities match;
- durable branch and worktree are recorded;
- old temporary worktree is explicitly marked lost;
- baseline suite is freshly executed in isolated HOME with zero unexpected failures;
- WP-00..WP-22 DAG is represented without inventing Phase A-G authority;
- exact WP-00 writable scope and forbidden runtime scope are recorded;
- control plane is not Product Memory;
- `git diff --check` and local checkpoint succeed.
