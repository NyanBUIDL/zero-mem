# WP-08 Task Packet — Agent-Agnostic Public API

Authority: planning ref, WP-08, ADR-001/006, current source.

Production call graph: generic caller → public `zero_mem` client/facade → existing capture/access/retrieval/context owners; Hermes remains an adapter consumer.

Exact scope: `zero_mem/core.py`, `zero_mem/api.py`, `zero_mem/__init__.py`, direct existing subsystem modules only where required, `tests/unit/test_wp08_public_api.py`, `tests/integration/test_wp08_generic_client.py`, `benchmarks/wp08_api.py`, API contract/evidence/handoff/state artifacts. Forbidden: Hermes-specific semantics in core, raw storage exposure, Product Memory, remote Git, post-MVP write/project capabilities.

Gates: public-only generic lifecycle, four capability contract, typed errors, idempotency/ownership, disabled/failure behavior, no `src` imports for generic users.
