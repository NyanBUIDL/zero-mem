# WP-01 Task Packet — Architecture Contract

## Authority

Derived from planning ref `2f1763f1335326b83bf90f263cada5c3715f21eb`, `MASTER_PLAN.md`, WP-01, ADR-001..005, and direct source inspection at baseline `78c4bb46b88b8ce9987c6882b24201e08b82a7f0`.

## Exact current scope

This activation is architecture/control-plane scope only: `artifacts/tasks/WP-01.md`, `artifacts/codemaps/WP-01-architecture.md`, `artifacts/evidence/WP-01-*.md`, `artifacts/handoffs/WP-01-*.md`, `prompts/phases/WP-01.md`, and `project-state.yaml` overlay. Runtime source, tests, packaging, schema, dependency, and configuration paths remain forbidden until a task-local implementation envelope is materialized.

## Production call graph

`Hermes plugin context.register_hook` → `RegistrationAdapter.register` → callback from `_make_callback` → `RegistrationAdapter._observe` → `map_hook_payload` → `adapt_mapped_event` → injected `store.append` → `JsonlCaptureStore.append`.

Derived-state path is separate: explicit `ingest_file` → SQLite metadata/index projection. Public release package currently exports version only (`zero_mem/__init__.py`), so no public lifecycle caller exists yet.

## Frozen gates

- owner/interface/state for major components is explicit;
- core remains free of Hermes/transport imports;
- one facade/lifecycle owner is proposed without duplicating storage/retrieval/authorization;
- composite canonical trace versus rebuildable derived state remains explicit;
- WP-08 owns the transport-neutral capability contract, WP-07 only Hermes mapping, WP-21 local transport, WP-20 profile semantics, WP-22 Obsidian, WP-14 conflict lifecycle;
- no circular WP/module dependency;
- architecture artifacts are source-bound and later WPs remain untouched.

## Decision boundary

WP-01 does not invent detailed API signatures owned by WP-08. Any runtime facade implementation must be a separately materialized task with exact paths and real-entry-point tests.
